package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Darker shade of white for outdoor canvas so pure white headers and cards stand out clearly
val BackgroundNeutral = Color(0xFFE5E8ED)
val SurfaceCard = Color(0xFFFFFFFF)
val SurfaceCardVariant = Color(0xFFF1F4F8)
val CardBorderColor = Color(0xFFD3D8E2)

// Primary Accent for actions (New Report, Add Issue, Add Photo, Save)
val PrimaryAccent = Color(0xFF1D4ED8) // Bold Inspection Blue
val PrimaryAccentDark = Color(0xFF1E40AF)
val PrimaryAccentLight = Color(0xFFEFF6FF)

// Destructive
val DestructiveRed = Color(0xFFDC2626)
val DestructiveRedLight = Color(0xFFFEF2F2)
val DestructiveRedBorder = Color(0xFFFECACA)

// Text Colors (High contrast outdoor legibility)
val TextPrimary = Color(0xFF0F172A)
val TextSecondary = Color(0xFF475569)
val TextTertiary = Color(0xFF64748B)

// Status Pill Colors
// In Progress: Amber
val StatusInProgressBg = Color(0xFFFEF3C7)
val StatusInProgressText = Color(0xFF92400E)
val StatusInProgressBorder = Color(0xFFFCD34D)

// Completed: Green
val StatusCompletedBg = Color(0xFFDCFCE7)
val StatusCompletedText = Color(0xFF15803D)
val StatusCompletedBorder = Color(0xFF86EFAC)

// Sent to Client: Emerald / Cyan
val StatusSentBg = Color(0xFFCCFBF1)
val StatusSentText = Color(0xFF115E59)
val StatusSentBorder = Color(0xFF5EEAD4)

// Draft: Neutral Gray
val StatusDraftBg = Color(0xFFF1F5F9)
val StatusDraftText = Color(0xFF475569)
val StatusDraftBorder = Color(0xFFCBD5E1)

// Awaiting Review: Distinct Purple / Indigo
val StatusReviewBg = Color(0xFFEDE9FE)
val StatusReviewText = Color(0xFF5B21B6)
val StatusReviewBorder = Color(0xFFC4B5FD)
